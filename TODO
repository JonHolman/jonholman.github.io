Add timer to mupltiplication page (upper left), when it hits 0 - display full score saying time is up and what their score is
- include list of ones they had trouble with



Look into forcing the pages to only display in landscape, via CSS

Move CSS into another file
